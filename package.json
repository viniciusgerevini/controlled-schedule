{
  "name": "controlled-schedule",
  "version": "1.0.0",
  "description": "Recurring schedule. Schedule next execution only after the current one finishes",
  "main": "lib/controlled-schedule.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha",
    "lint": "./node_modules/.bin/eslint ./",
    "pretest": "npm run lint",
    "coverage": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha"
  },
  "keywords": [
    "schedule",
    "interval",
    "timeout"
  ],
  "author": "Vinicius Gerevini <viniciusgerevini@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/viniciusgerevini/controlled-schedule"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.9",
    "eslint": "^2.9.0",
    "istanbul": "^0.4.3",
    "mocha": "^2.4.5"
  },
  "dependencies": {
    "ms": "^0.7.1"
  },
  "engines": {
    "node": ">=4.1.0"
  }
}
